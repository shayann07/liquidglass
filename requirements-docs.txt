# The documentation site. Pinned on purpose: MkDocs 2.0 removes the plugin system and rewrites
# theming with no migration path, so an unpinned build would break without a commit here.
# Upgrade deliberately, and check `mkdocs build --strict` locally first.
mkdocs==1.6.1
mkdocs-material==9.7.7
pymdown-extensions==11.0.2
